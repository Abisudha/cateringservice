import { Component } from '@angular/core';

@Component({
  selector: 'app-menu',
  templateUrl: './menu.component.html',
  styleUrls: ['./menu.component.scss']
})
export class MenuComponent {
collapseAllTabs(arg0: string) {
throw new Error('Method not implemented.');
}

}



        // var openedBtn = "";
        
        // function collapseAllTabs(clickedBtnId){

            // if(openedBtn != "")
                // document.getElementById(openedBtn).click();
            // openedBtn = clickedBtnId;
        // }
    